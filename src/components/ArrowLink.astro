---
interface Props {
  href: string;
  external?: boolean;
  class?: string;
}

const { 
  href,
  external = false,
  class: className 
} = Astro.props;

const externalProps = external ? {
  target: "_blank",
  rel: "noopener noreferrer"
} : {};
---

<a 
  href={href}
  class:list={[
    "group inline-flex items-center gap-1",
    "text-content-muted hover:text-primary dark:text-gray-400 dark:hover:text-primary transition-colors duration-200 text-lg",
    "no-underline-link focus-ring",
    className
  ]}
  {...externalProps}
>
  <slot />
  {external && <span class="sr-only"> (opens in new tab)</span>}
  <span 
    class="group-hover:translate-x-1 transition-transform"
    aria-hidden="true"
  >
    →
  </span>
</a> 